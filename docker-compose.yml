version: '3.4'

services:
  catalogdb:
    image: mongo
  mscatalog:
    image: ${DOCKER_REGISTRY-}mscatalog
    build:
      context: .
      dockerfile: MsCatalog/Dockerfile

volumes:
    mongo_data: 